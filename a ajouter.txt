MDP secret du site ovh : 6dapkRdfd6e5 
Mon mot de passe unbuntu : sitewebdeKelly1245

mdp de la base : baseKelly2026+

pour se reconnecter ssh ubuntu@152.228.141.247

étape 1, fait la mise a jour :
sudo apt update && sudo apt upgrade -y

étape 2, installe les dépendances : 
sudo apt install -y curl git nginx certbot python3-certbot-nginx mysql-server


ALTER USER 'kelly'@'localhost' IDENTIFIED BY 'baseKelly2026+';
FLUSH PRIVILEGES;
EXIT;


DB_HOST=localhost
DB_PORT=3306
DB_USER=kelly
DB_PASSWORD=baseKelly2026+
DB_NAME=annivkelly
JWT_SECRET=aK9#mP2$xL7nQ4vR8wT1yB6uF3jE5sH0
JWT_EXPIRES_IN=7d
SPOTIFY_CLIENT_ID=98c637129d9f46968408ff61a9b8da9d
SPOTIFY_CLIENT_SECRET=f038197a3d734750b012f4ccfc464905
PORT=3001


### Pour mettre à jour le site

git add .
git commit -m "Description de ce que t'as changé"
git push
2. Sur le VPS — connecte-toi en SSH puis :

cd /var/www/annivkelly
sudo git pull
Si t'as modifié le frontend :

cd /var/www/annivkelly/frontend
sudo npm run build
Si t'as modifié le backend :

sudo pm2 restart annivkelly-backend
C'est tout ! En gros : push depuis ton PC → pull sur le VPS → r